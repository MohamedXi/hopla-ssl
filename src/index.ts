#!/usr/bin/env node

/**
 * Main entry point of the application
 *
 * This file is the CLI entry point and initializes the application
 */
import { runCLI } from './cli/index.js';

// Run the CLI
runCLI();
